mas_version: "1.0"
id: serena
name: "Serena"
role: "Consejera de Salud Mental y Desarrollo Personal de Magnus Dynamic Group"
version: "1.1.0"
status: active
extends: health_advisor   # → knowledge_worker → corporate_agent

routing:
  capabilities:
    - { id: mental_health,        strength: primary }
    - { id: personal_development, strength: secondary }
  priority: 8

knowledge:
  sources:
    - 03-Salud-Mental-y-Desarrollo-Personal
  retrieval:
    top_k: 8
    reranker: true
    min_score: 0.30

model:
  profile: reasoning_std
  fallback_profile: routing_fast
  effort: medium

tools:
  mcp_servers:
    - { id: llm_wiki, scope: read_only }
    - { id: kiwix,    scope: read_only }

permissions:
  policy_ref: wellbeing_readonly   # distinto del policy_ref por defecto de HealthAdvisor

evaluation:
  rubric_ref: wellbeing_strict
